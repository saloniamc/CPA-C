#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)
{
    char input;
    printf("Enter a character from the keyboard: \n");
    input = _getch();
    printf("The entered character is '%c'\n", input);

    exit(0);
}

#include < stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)
{
    char input;
    printf("Enter the character \n");
    input = _getch();
    printnf("The entered character is %c\n", input);

    exit(0);
}

#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)
{
    char input;
    printf("Enter any character \n");
    input = _getch();
    printf("The character is %c", input);
    exit(0);
}

#include <Stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)
{
    char in;
    printf("Enter the character \n");
    in = _getch();
    printf("The entered character is %c \n", in);
    exit(0);
}

#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(void)
{
    char input;
    printf("Enter any character \n");
    input = _getch();
    printf("The entered character is %c\n", input);
    exit(0);
}
