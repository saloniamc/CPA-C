#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int a = 10;
    int b = 20;
    int c = 3;
    int d = 2;
    int rs;
    rs = a * c + b * d;

    printf("rs = %d\n", rs);

    return (0);
}